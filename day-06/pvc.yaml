apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    pvc: primeiro-pvc
  name: meu-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
  matchLabels:
    storage: nfs